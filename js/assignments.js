document.addEventListener('DOMContentLoaded',async()=>{const u=await AlfredCore.requireStudent();if(!u)return;const root=document.getElementById('tasks');try{const ts=await AlfredCore.listTasks();root.innerHTML=ts.map(t=>`<div class="card"><span class="tag">TUGAS GURU</span><h2>${AlfredCore.esc(t.title)}</h2><p>${AlfredCore.esc(t.description||'')}</p><p class="muted">Deadline: ${AlfredCore.esc(t.deadline||'-')} • ${t.points||100} poin</p><textarea id="a-${t.id}" placeholder="Tulis jawaban / hasil tugas..."></textarea><button class="btn gold submit" data-id="${t.id}">Kirim Tugas</button></div>`).join('')||'<div class="notice">Belum ada tugas dari guru.</div>';document.querySelectorAll('.submit').forEach(b=>b.onclick=async()=>{try{await AlfredCore.submitTask(b.dataset.id,document.getElementById('a-'+b.dataset.id).value);b.textContent='Terkirim ✓';b.disabled=true}catch(e){alert(e.message)}})}catch(e){root.innerHTML='<div class="notice danger">Gagal memuat tugas.</div>'}});
