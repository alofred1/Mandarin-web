document.addEventListener('DOMContentLoaded',async()=>{const root=document.getElementById('rows');try{const a=await AlfredCore.top100();root.innerHTML=a.length?a.map((p,i)=>`<div class="rank"><b>${i<3?['🥇','🥈','🥉'][i]:'#'+(i+1)}</b><span><b>${AlfredCore.esc(p.username)}</b><small class="muted">Level ${p.level} • ${p.totalGames} game</small></span><strong>${p.xp.toLocaleString('id-ID')} XP</strong><small>#${i+1}</small></div>`).join(''):'<div class="notice">Belum ada siswa.</div>'}catch(e){root.innerHTML='<div class="notice danger">Leaderboard gagal dimuat: '+AlfredCore.esc(e.message)+'</div>'}});
